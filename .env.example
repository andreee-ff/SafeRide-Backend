# Database Configuration
# PostgreSQL (recommended for production/Docker usage)
DATABASE_URL=postgresql://saferide_user:saferide_password@localhost:5432/saferide_db

# SQLite (for development without Docker or local testing)
# DATABASE_URL=sqlite+aiosqlite:///./ride.db

# Testing (Environment for running pytest)
# TEST_DATABASE_URL=sqlite+aiosqlite:///./test.db

# Security Settings
# Generate a secure random secret for production usage:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev_secret_key_ensure_you_change_this_in_production
